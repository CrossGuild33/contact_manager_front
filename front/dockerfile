# Step 1: Use a lightweight nginx image as the base
FROM nginx:alpine

# Step 2: Copy the current directory (your JS app) to the nginx html directory
COPY . /usr/share/nginx/html

# Step 3: Expose the port nginx will serve on
EXPOSE 80

# Step 4: Start nginx server
CMD ["nginx", "-g", "daemon off;"]